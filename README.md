# platform-libraries

Monorepo for shared platform Python libraries.

## Libraries
- `polyllm`: provider-agnostic LLM routing + initialization layer (LangChain-backed)

## Dev (uv)
```bash
uv sync
uv run pytest